{
  "name": "ryotaroportfolio",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "check:css": "node scripts/check-css-health.mjs",
    "test:smoke": "npm run build && node scripts/smoke-routes.mjs",
    "check": "npm run check:css && npm run test:smoke"
  },
  "devDependencies": {
    "vite": "^7.0.0"
  }
}
